@model IEnumerable<CarritoDeCompras.Models.Categoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Categorías</h1>

@{
    if(ViewBag.Mensaje != null)
    {
        <div class="form-floating mb-3">
            <div class="alert alert-danger" role="alert">
                @ViewBag.Mensaje
            </div>
        </div>
    }
}

<div>
    <a asp-action="Create" class="btn btn-primary" style="float: right">Crear nueva categoría</a>
</div>
<br />

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaRegistro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCategoria" title="Editar categoría"><i class="fa-solid fa-pen-to-square"></i></a> |
                <a asp-action="Details" asp-route-id="@item.IdCategoria" title="Ver detalle"><i class="fa-solid fa-circle-plus"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdCategoria" title="Eliminar categoría"><i class="fas fa-times"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
